version: "1.2"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
      - "3000:3000"
    environment:
      - ENV=production
    command: npm run dev

